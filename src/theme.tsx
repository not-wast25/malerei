import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "'Oswald', sans-serif", // Hier die gewünschte Schriftart angeben
  },
});

export default theme;
